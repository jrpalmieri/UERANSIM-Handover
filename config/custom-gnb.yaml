mcc: '286'          # Mobile Country Code value
mnc: '01'           # Mobile Network Code value (2 or 3 digits)

nci: '0x000000010'  # NR Cell Identity (36-bit)
idLength: 32        # NR gNB ID length in bits [22...32]
tac: 1              # Tracking Area Code

linkIp: 127.0.0.1   # gNB's local IP address for Radio Link Simulation (Usually same with local IP)
ngapIp: 127.0.0.1   # gNB's local IP address for N2 Interface (Usually same with local IP)
gtpIp: 127.0.0.1    # gNB's local IP address for N3 Interface (Usually same with local IP)

# List of AMF address information
amfConfigs:
  - address: 127.0.0.5
    port: 38412

# List of supported S-NSSAIs by this gNB
slices:
  - sst: 1
    sd: 1

# Indicates whether or not SCTP stream number errors should be ignored.
ignoreStreamIds: true

# RSRP configuration:
#   dbValue: initial RSRP value (dBm) used in Fixed mode and as the starting fixed value
#   updateMode: Calculated (distance/path-loss based) or Fixed (ignore distance)
rsrp:
  dbValue: -120
  updateMode: Calculated

# Handover measurement event configuration.
# eventType accepts either a single value or a list: A2, A3, A5.
handover:
  eventType: [A3]
  a2ThresholdDbm: -110
  a3OffsetDb: 3
  a5Threshold1Dbm: -110
  a5Threshold2Dbm: -95
  hysteresisDb: 1
  xn:
    enabled: false
    bindAddress: 127.0.0.1
    bindPort: 9487
    requestTimeoutMs: 1000
    contextTtlMs: 5000
    fallbackToN2: true
