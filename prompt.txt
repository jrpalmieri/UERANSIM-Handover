now change the visualization UI.  the visualization should propagate all satellites in the TLE store.  It shoudl determine which sats are hosting a gnb.  satellites that are not associated with a GNB shoudl be displayed in the graphics as a small white dot.  satellites associated with a GNB shoudl be displayed as currently displayed - coloring, label and larger dot.  The unassociated sats shoudl only be listed in the location data pane if they are above the UE horizon.  In the location data pane, the enties shoudl be labelled as "entity key (gnb_name - sat name)" : example: GNB1 (gnb-0017 - STARLINK-1008).  If the entity is a sat that is unassociated with a GNB, then just use the sat name as the label.  For unassociated sata, only display the propagated location information.  For sats hosting a gnb, switch teh order of the "reported/propagated" values, so that propagated comes first (and thereby lines up with teh values of any sats that are unassociated with gnbs).  sats that are unassociated with a gnb shodul be propagated using the earliest of teh reported sat-time values from the gnbs.