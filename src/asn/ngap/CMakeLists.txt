# Generated by asn1c-r18 with -fprefix=ASN_NGAP_ -fcommon: generated code only.
# The skeleton runtime lives once in src/asn/asn1c and these headers reach it by
# relative path ("../asn1c/..."). Do not add skeleton files here.
file(GLOB HDR_FILES CONFIGURE_DEPENDS ASN_NGAP_*.h)
file(GLOB SRC_FILES CONFIGURE_DEPENDS ASN_NGAP_*.c)

add_library(asn-ngap ${HDR_FILES} ${SRC_FILES})
target_include_directories(asn-ngap PUBLIC ${CMAKE_CURRENT_SOURCE_DIR})
target_link_libraries(asn-ngap asn-asn1c)
